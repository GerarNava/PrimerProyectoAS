@model IEnumerable<PrimerProyecto.LUGAR>

@{
    ViewBag.Title = "LUGAR";
}

<h2>LUGAR</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Dirección)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Nom_Lugar)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DISTRITO.Nom_Distrito)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Dirección)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Nom_Lugar)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DISTRITO.Nom_Distrito)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_lugar }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_lugar }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_lugar })
        </td>
    </tr>
}

</table>
