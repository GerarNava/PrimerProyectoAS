@model IEnumerable<PrimerProyecto.PRECIOS_EVENTOS>

@{
    ViewBag.Title = "PRECIOS DEL EVENTO";
}

<h2>PRECIO EVENTO</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.precioEvento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.precioEvento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Precio }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Precio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Precio })
        </td>
    </tr>
}

</table>
