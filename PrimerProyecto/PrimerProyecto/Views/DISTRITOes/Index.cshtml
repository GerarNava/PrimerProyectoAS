@model IEnumerable<PrimerProyecto.DISTRITO>

@{
    ViewBag.Title = "DISTRITO";
}

<h2>DISTRITO</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Nom_Distrito)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PAIS.NomPais)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Nom_Distrito)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PAIS.NomPais)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_distrito }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_distrito }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_distrito })
        </td>
    </tr>
}

</table>
