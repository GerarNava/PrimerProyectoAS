@model IEnumerable<PrimerProyecto.PAIS>

@{
    ViewBag.Title = "PAIS";
}

<h2>PAIS</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NomPais)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NomPais)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id_Pais }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id_Pais }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id_Pais })
        </td>
    </tr>
}

</table>
